# S.L.A.T.E. Requirements
# Author: COPILOT | Modified: 2026-02-08T21:30:00Z
# Install: pip install -r requirements.txt
# Lock:    See requirements.lock for exact pinned versions
# Note:    PyTorch CUDA is installed separately in Dockerfile (not via requirements.txt)

# ─── Core ────────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0,<2
psutil>=5.9.0,<8
rich>=13.0.0,<15
typing-extensions>=4.0.0,<5

# ─── Web/API (Dashboard Server) ──────────────────────────────────────────────
fastapi>=0.109.0,<1
python-multipart>=0.0.7
uvicorn[standard]>=0.27.0,<1
websockets>=12.0,<17
aiohttp>=3.9.0,<4
httpx>=0.26.0,<1

# ─── Dev Hot-Reload ──────────────────────────────────────────────────────────
watchfiles>=1.0.0,<2

# ─── Vector Store (RAG Memory) ───────────────────────────────────────────────
chromadb>=0.4.22,<2

# ─── AI Toolkit (REQUIRED for SLATE inference pipeline) ──────────────────────
# Modified: 2026-02-08T21:30:00Z | Author: COPILOT | Change: Uncomment AI deps - required for Docker runtime
transformers>=4.40.0,<6
accelerate>=0.30.0,<2
# torch — installed separately with CUDA support (see Dockerfile)
# For local CPU-only dev: pip install torch --index-url https://download.pytorch.org/whl/cpu
# For CUDA: pip install torch --index-url https://download.pytorch.org/whl/cu128

# ─── Semantic Kernel (Microsoft AI Orchestration) ────────────────────────
semantic-kernel[ollama,hugging_face,chroma]>=1.39.0,<2
sentence-transformers>=5.0.0,<6

# ─── Microsoft Defender / Security Analysis ──────────────────────────────
msticpy>=2.18.0,<3
azure-mgmt-security>=7.0.0,<8

# ─── Copilot SDK (GitHub Copilot CLI Integration) ───────────────────────────
github-copilot-sdk>=0.1.0

# ─── Tracing/Observability ───────────────────────────────────────────────────
opentelemetry-api>=1.20.0,<2
opentelemetry-sdk>=1.20.0,<2
opentelemetry-exporter-otlp>=1.20.0,<2

# ─── Testing ─────────────────────────────────────────────────────────────────
pytest>=8.0.0,<10
pytest-asyncio>=0.23.0,<2
pytest-cov>=4.1.0,<8

# ─── Development ─────────────────────────────────────────────────────────────
ruff>=0.2.0,<1
mypy>=1.8.0,<2
