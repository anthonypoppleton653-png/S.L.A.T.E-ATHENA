# Modified: 2026-02-07T04:56:45Z | Author: COPILOT | Change: Normalize paths and clean workflow artifacts
# CodeQL Security Analysis

name: CodeQL Analysis

on:
  push:
    branches: [main, develop]
    paths:
      - '**.py'
      - 'slate/**'
      - 'agents/**'
  pull_request:
    branches: [main]
    paths:
      - '**.py'
  schedule:
    - cron: '30 3 * * 1'
  workflow_dispatch:

# Modified: 2026-02-07T08:30:00Z | Author: COPILOT | Change: Fix concurrency for mixed push/PR/schedule triggers
concurrency:
  group: codeql-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: false

permissions:
  actions: read
  contents: read
  security-events: write

defaults:
  run:
    shell: powershell

jobs:
  analyze:
    name: CodeQL Analysis
    runs-on: [self-hosted, slate]
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        language: ['python']
    steps:
      - uses: actions/checkout@v6
      - uses: github/codeql-action/init@v4
        with:
          languages: ${{ matrix.language }}
          queries: +security-extended,security-and-quality
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
        continue-on-error: true
      - uses: github/codeql-action/analyze@v4
        with:
          category: '/language:${{ matrix.language }}'
