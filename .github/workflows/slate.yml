# 
# SLATE System Integration Workflow
# Author: COPILOT | Created: 2026-02-02T00:00:00Z | Modified: 2026-02-06T14:00:00Z
# Validates SLATE-specific subsystems: tech tree, nemo, dashboard, agents, tasks
# 

name: SLATE Integration

on:
  push:
    branches: [main, develop, '001-*', '002-*', '003-*', '004-*']
    paths:
      - 'aurora_core/**'
      - 'agents/**'
      - '.slate_tech_tree/**'
      - '.slate_nemo/**'
      - 'current_tasks.json'
      - 'pyproject.toml'
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: slate-${{ github.ref }}
  cancel-in-progress: true

env:
  PYTHON_VERSION: '3.11'

permissions:
  contents: read

jobs:
  slate-status:
    name: SLATE Status Check
    runs-on: windows-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
        continue-on-error: true
      - name: Run SLATE Status
        run: python aurora_core/slate_status.py --quick
        continue-on-error: true

  sdk-validation:
    name: SDK Validation
    runs-on: windows-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
        continue-on-error: true
      - name: Validate SDK version sync
        run: |
          python -c "
          import tomllib
          with open('pyproject.toml', 'rb') as f:
              c = tomllib.load(f)
          toml_ver = c['project']['version']
          import aurora_core
          sdk_ver = aurora_core.__version__
          print(f'pyproject.toml: {toml_ver}')
          print(f'aurora_core:    {sdk_ver}')
          assert toml_ver == sdk_ver, f'VERSION MISMATCH: {toml_ver} != {sdk_ver}'
          print('Version sync: OK')
          "

  tech-tree-validation:
    name: Tech Tree Validation
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      - name: Validate Tech Tree JSON
        run: |
          python -c "
          import json, sys
          with open('.slate_tech_tree/tech_tree.json', 'r') as f:
              tree = json.load(f)
          assert 'nodes' in tree, 'Missing nodes key'
          assert 'metadata' in tree, 'Missing metadata key'
          nodes = tree['nodes']
          completed = sum(1 for n in nodes if n.get('status') == 'complete')
          total = len(nodes)
          progress = (completed / total * 100) if total > 0 else 0
          print(f'Tech Tree: {completed}/{total} nodes complete ({progress:.1f}%)')
          "

  dashboard-smoke-test:
    name: Dashboard Smoke Test
    runs-on: windows-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
        continue-on-error: true
      - name: Test Dashboard Import
        run: |
          python -c "
          try:
              from agents.aurora_dashboard_server import app
              print('Dashboard module imports successfully')
          except ImportError as e:
              print(f'Dashboard import failed: {e}')
              exit(1)
          "

  agent-initialization:
    name: Agent Initialization Test
    runs-on: windows-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
        continue-on-error: true
      - name: Test Agent Module Imports
        run: |
          python -c "
          import sys
          errors = []
          modules = [
              'aurora_core.slate_status',
              'aurora_core.smart_task_router',
              'aurora_core.feature_flags',
              'aurora_core.action_guard',
              'aurora_core.build_versioning',
          ]
          for mod in modules:
              try:
                  __import__(mod)
                  print(f'OK {mod}')
              except Exception as e:
                  print(f'FAIL {mod}: {e}')
                  errors.append(mod)
          if errors:
              print(f'Failed to import {len(errors)} modules')
              sys.exit(1)
          else:
              print('All agent modules import successfully')
          "

  task-queue-check:
    name: Task Queue Validation
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - name: Validate Task Queue
        run: |
          python -c "
          import json
          with open('current_tasks.json', 'r') as f:
              tasks = json.load(f)
          pending = [t for t in tasks if t.get('status') == 'pending']
          in_progress = [t for t in tasks if t.get('status') == 'in_progress']
          completed = [t for t in tasks if t.get('status') == 'completed']
          print(f'Task Queue: {len(pending)} pending, {len(in_progress)} in-progress, {len(completed)} completed')
          print(f'Total: {len(tasks)} tasks')
          "

  summary:
    name: SLATE Summary
    needs: [slate-status, sdk-validation, tech-tree-validation, dashboard-smoke-test, agent-initialization, task-queue-check]
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Summary
        run: |
          echo "## SLATE Integration Results" >> $GITHUB_STEP_SUMMARY
          echo "| Check | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|-------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| SLATE Status | ${{ needs.slate-status.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| SDK Validation | ${{ needs.sdk-validation.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Tech Tree | ${{ needs.tech-tree-validation.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Dashboard | ${{ needs.dashboard-smoke-test.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Agents | ${{ needs.agent-initialization.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Task Queue | ${{ needs.task-queue-check.result }} |" >> $GITHUB_STEP_SUMMARY
